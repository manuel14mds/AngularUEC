import { MainTitleDirective } from './main-title.directive';

describe('MainTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new MainTitleDirective();
    expect(directive).toBeTruthy();
  });
});
